import 'package:flutter/material.dart';

class DoctorIdPage extends StatelessWidget {
  const DoctorIdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
